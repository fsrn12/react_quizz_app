import NextButton from "./NextButton";
import Timer from "./Timer";

const Footer = function () {
  return (
    <footer>
      <Timer />
      <NextButton />
    </footer>
  );
};

export default Footer;
